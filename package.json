{
  "name": "adult-kink-exploration-prototype",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test scripts/*.test.mjs && npm run validate:activity-catalog && npm run validate:fantasy-profile",
    "validate:fantasy-profile": "node scripts/fantasy-profile-validation.mjs",
    "validate:activity-catalog": "node scripts/activity-catalog-validation.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
